---
    type Props = {
        color?: string;
        size?: string;
        className?: string
        style?: string
    }

    const { color = "currentColor", size = 24, className, style } = Astro.props
---